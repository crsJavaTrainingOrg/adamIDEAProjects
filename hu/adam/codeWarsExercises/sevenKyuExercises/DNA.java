package hu.adam.codeWarsExercises.sevenKyuExercises;

/*
public class DNA {
    public static String decomposeDoubleStrand(final String doubleStrand) {
        String reverseComplement = "";
        char[] letters = doubleStrand.toCharArray();

*/
/*        for (int i = 0; i < doubleStrand.length(); i++) {
            if (doubleStrand.charAt(i) == 'A') {
                reverseComplement = reverseComplement + 'T';
            } else if (doubleStrand.charAt(i) == 'T') {
                reverseComplement = reverseComplement + 'A';
            } else if (doubleStrand.charAt(i) == 'G') {
                reverseComplement = reverseComplement + 'C';
            } else if (doubleStrand.charAt(i) == 'C') {
                reverseComplement = reverseComplement + 'G';
            }

        }
        String reversedReverse = new StringBuilder(reverseComplement).reverse().toString();*//*

        String frameOne = "";
        for(int i = 0; i < letters.length; i++) {
            frameOne = String.format("%C%C%C %C%C%C %C%C%C %C%C%C %C%C%C %C%C%C %C%C%C %C%C%C %C%C%C", '');
        }
            System.out.println(frameOne);

        return reverseComplement;
    }
}
*/
